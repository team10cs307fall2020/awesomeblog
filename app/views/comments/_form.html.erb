<%= form_for([@post, @comment]) do |f| %>
  <p>
    <%= f.label :commenter %><br>
    <%= f.label current_user.username %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>

<% end %>