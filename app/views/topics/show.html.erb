<p>
  <%= "New topic Created Successfully!" %>
</p>


<p>
  <strong>Topic Name:</strong> <br>
  <%= @topic.title %>
</p>

<p>
  <strong>Topic Description:</strong> <br>
  <%= @topic.desc %>
</p>

<p>
  <strong>Topic Post:</strong> <br>

</p>



<table>
  <tr>
  <th>Title</th>
  <th>Text</th>
  </tr>


  <% for i in 0..Post.where(:topic => @topic.title).to_a.pluck(:title, :text).length - 1%>
    <tr>
      <td> <%= @post = Post.where(:topic => @topic.title).pluck(:title, :text)[i][0] %></td>
      <td> <%= @post = Post.where(:topic => @topic.title).pluck(:title, :text)[i][1] %></td>
    </tr>
  <% end %>

  </table>



<%= link_to 'Edit', edit_topic_path(@topic) %> |
<%= link_to 'Back', topics_path %> | <%= link_to 'Check', posts_path %>

